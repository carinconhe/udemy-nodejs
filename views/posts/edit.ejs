<h1>Edit Post!</h1>
<form action="/posts/<%=post.id%>?_method=PUT" method="POST" enctype="multipart/form-data">
    <div><input type="text" name="post[title]" placeholder="Title" value="<%= post.title%>"></div>
    <div><input type="text" name="post[price]" placeholder="Price" value="<%= post.price%>"></div>
    <div>
        <input type="file" accept="images/*" name="images" multiple />
        <div>
            <%post.images.forEach(function(image){%>
                <img src="<%= image.url%>" alt="name image" width="100px">
            <%});%>
        </div>
    </div>
    <div>
        <textarea name="post[description]" placeholder="Description" id="" cols="30" rows="10"><%= post.description%></textarea>
    </div>
    <div><input type="text" name="post[location]" placeholder="Location" value="<%= post.location%>"></div>
    <div><input type="submit" value="Submit"></div>
</form>  